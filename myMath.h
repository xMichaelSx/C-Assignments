
 // returns x + y
float add(float x , float y);

 // returns x - y
float sub(float x , float y);

 // returns y*x
double mul(double x , int y);

//  returns x/y
double div(double x, int y);

double Exponent(int x); // e

double Power(double x , int y);

// Note: i assume its ok to make some helper functions as its the same as 
//       including big libraries, so this is more efficient.

void empty_stdin(void); // helper function to empty stdin after wrong input for getting input again.

double str_to_double(const char* arr); // built help func to convert str input to double number.